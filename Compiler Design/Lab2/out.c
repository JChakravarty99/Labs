/*Always begin programming
with the main function*/

int main()
{
	//simple variable declaration can also be demonstrated here
	int 		a, 				b;
	float     c;
	a= 		6;
	b 	=  8;
	c =  9.9;
	printf    ("%d        %d          %f\n",a,b,c); 
	/*print a simple hello message!*/
	printf              ("Hello World!\n");  //A simple hello message!
	/* The printf command is the backbone to most of C's I/O capabilities. 
	It uses the console to print outputs to the code you run.*/
	
	//edge cases
	printf("Checking preprocessor.c #include <stdio.h> \" #define PORT 8000");
	if('		printf("Edge case test passed!");
	return 0;
}                                                                        